Enhancement: Add --tree option to diff command

Restic diff can now print per-path blob statistics when called with the
`--tree` flag. The command emits a hierarchical JSON structure which lists,
for each path, how many blobs and bytes were added, removed and are common
between two snapshots.

https://github.com/restic/restic/pull/0000
